# Awesome Project Build with TypeORM

Steps to run this project:

1. Run `npm i` command
2. Setup database settings inside `data-source.ts` file
3. Run `npm start` command


# Things changed in the data files, to make them compatible:
A list of all changes to the data files, to recreate them for future use:

1. Remove all comments at the start
2. 